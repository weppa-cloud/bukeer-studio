/**
 * Shared display constants for website rendering.
 * Consumed by: web-public (Next.js site components).
 */

/** Maximum gallery images rendered in the product gallery section. */
export const PACKAGE_GALLERY_MAX = 12;

/** Maximum inclusions listed in the include/exclude section. */
export const PACKAGE_INCLUSIONS_MAX = 15;

/** Maximum amenity badges rendered per hotel itinerary item. */
export const HOTEL_AMENITIES_MAX = 6;

/** Number of schedule steps shown before "Ver N más" toggle. */
export const SCHEDULE_STEPS_VISIBLE = 5;
