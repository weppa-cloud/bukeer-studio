# T1 PLAN GATE — Technical Validation Brief

**Task:** t_56655041 — [growth-control-plane-phase1] validate DB/canary safety
**Validator:** Bukeer Tech Validator (MODE:TASK)
**SPEC:** `docs/specs/SPEC_GROWTH_CONTROL_PLANE_PHASE1_MIGRATIONS.md`
**Validation Date:** 2026-05-18
**Verdict:** PASS_WITH_MINOR

## Validation Gates

### 1. Product boundary — control plane stays in Bukeer Studio → PASS

- 6 tables are all Supabase/Bukeer Studio tables (operational SSOT per SPEC_GROWTH_OS_SSOT_MODEL)
- Workers consume ContextPacket only (Worker Contract Rule 2) — explicitly restated as SPEC safety rule #5
- Workers never read new tables directly; context-builder does on their behalf (existing `selectRows` pattern)
- `growth_context_packet_log` records ContextPackets dispatched to workers — audit layer, not worker-facing

### 2. RLS/multi-tenant isolation → PASS

- All 6 tables have `account_id` and `website_id` FK constraints matching ADR-009
- RLS pattern matches existing `20260509120000` migration exactly:
  - `ENABLE ROW LEVEL SECURITY`
  - Service role policy: full access (`_service_all`)
  - Authenticated users: SELECT-only via `user_roles` tenant scoping (`_tenant_read`)
  - Writes restricted to `service_role`
- INDEXes on `(website_id, locale, market)` for tenant-scoped lookups

### 3. No provider calls → PASS

- Safety rule #1: "No provider API calls in any migration, backfill, or worker code"
- Safety rule #3: "No deploy to production until explicit human approval"
- `growth_agent_definitions.blocked_actions` defaults to `ARRAY['call_provider_api_directly']` — matches context-builder.ts hard constraint list (line 262-267)
- Canary test explicitly verifies no provider URLs in migration SQL (acceptance gate AC-09)
- Context-builder already enforces `call_provider_api_directly`, `paid_mutation`, `experiment_activation` as blocked action classes

### 4. Exact locale/market → PASS

- `growth_capabilities`: UNIQUE (website_id, locale, market, lane, capability) — exact match enforced at schema level
- `growth_agent_definitions`: locale + market NOT NULL with explicit `locale_family_fallback (opt-in) → BLOCKED` resolution rule
- Context-builder `buildGrowthAgentContext()` BLOCKS missing locale (line 111: "implicit es-CO fallback is forbidden")
- Canary procedure verifies:
  - `es-CO → pt-BR/BR` = PASS
  - Missing locale = BLOCKED with explicit error
  - `pt-BR/CO` mismatch = BLOCKED
- Market CHECK includes 'BR' for all new tables (correct — existing `growth_context_snapshots` pre-BR migration doesn't)

**Minor note:** Backfill COALESCE uses `COALESCE(gpr.locale, 'es-CO')` which applies implicit fallback for rows that genuinely have NULL locale. This is pragmatic for existing data but should be verified: run a pre-backfill query to count rows with NULL locale before proceeding.

### 5. source_refs/freshness/policy gates → PASS

- `growth_source_refs` is INSERT-ONLY ledger with explicit freshness_status enum ('fresh','stale','expired','unknown') and valid_from/valid_until windows
- `growth_provider_policies` formalizes consent, rate limits, data usage policy per provider/provider_profile — replaces implicit Worker Contract enforcement
- Backfill maps `growth_profile_runs` provider types to `source` values and evaluates freshness deterministically
- Context-builder already selects growth_profiles by `valid_until` ordering (context-builder.ts line 156)
- Phase 1c (next step) will integrate new tables into context-builder reads

### 6. Canary isolation → PASS

- Single entity: ColombiaTours (website_id=894545b7-73ca-4dae-b76a-da5b6a3f8441), pt-BR/BR
- Seed data: `growth_capabilities` + `growth_agent_definitions` with `canary_only=true` and `enabled=true`
- Dry-run mode: SELECT COUNT + preview only, no actual writes
- 8 acceptance gates including idempotency, RLS isolation, block path verification
- Rollback: DELETE by website_id — clean, re-seedable
- No provider calls, no secrets in SQL

## Blocking Issues

**None.** All 6 gates pass.

## Minor Issues (non-blocking, fix before Phase 1c)

1. **Filename typo fixed by operator** — T0 originally produced `SPEC_GROWTH_CONTROL_PLANE_PLANE_PHASE1_MIGRATIONS.md` (double "PLANE"). Neo renamed it to `SPEC_GROWTH_CONTROL_PLANE_PHASE1_MIGRATIONS.md` before T2 so INDEX.md resolves correctly.

2. **CHECK constraints use shorthand** — SPEC uses `CHECK lane enum` and `CHECK market enum` rather than explicit named constraints like `growth_<table>_<column>_chk` (existing convention in `20260509120000` migration). Production migration must use named constraints.

3. **Lane values not enumerated** — SPEC uses `CHECK lane enum` for `growth_capabilities` and `growth_agent_definitions` but doesn't list allowed values. Existing pattern uses: `'orchestrator','technical_remediation','transcreation','content_creator','content_curator'`. Must be explicit.

4. **Backfill locale audit** — Before backfill, run a verification query: `SELECT COUNT(*) FROM growth_profile_runs WHERE website_id='894... AND locale IS NULL` to confirm how many rows get implicit es-CO fallback.

## Watch Items (inherited from T0, unchanged)

1. `growth_context_packet_log` vs `growth_context_snapshots` boundary needs implementation-level clarification
2. fact_id linking in backfill is a follow-up step (currently NULL with note)
3. Zod schemas for 6 tables must precede/accompany migration SQL (ADR-003)

## Recommendation

**PASS** for T1 PLAN GATE. Proceed to implementation with minor issues addressed before migration PR is merged. No architectural regressions found. Safety rules (no provider calls, no deploy, exact locale/market) are all enforced at schema + context-builder level.