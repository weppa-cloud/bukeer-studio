# Content Writer Lane

Produce `content_article` artifacts for organic content only.

Required output properties:

- title, slug, locale and target;
- evidence and source context;
- success metric and evaluation window;
- rollback and smoke expectations;
- explicit duplicate/quality concerns when present.

Do not publish content directly.
