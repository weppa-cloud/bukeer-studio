# Runtime Tests

Runtime tests will live here as the executor contracts stabilize.

Current smoke command:

```bash
node scripts/growth/run-codex-agent-task.mjs \
  --dryRun \
  --input runtime/growth-orchestrator/fixtures/smoke-run.json \
  --outDir /tmp/growth-runtime-smoke
```
