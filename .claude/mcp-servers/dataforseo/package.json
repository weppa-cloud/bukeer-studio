{
  "name": "@bukeer/mcp-dataforseo",
  "version": "0.1.0",
  "private": true,
  "description": "Local MCP server wrapping DataForSEO API (SERP + keyword volume) with file-based cache and monthly budget cap.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-dataforseo": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
